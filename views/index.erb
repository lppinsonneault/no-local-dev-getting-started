<div class="container">
  <table class="table table-striped table-bordered">
  <tr>
    <th>Id</th>
    <th>Subject</th>
  </tr>

  <% @ideas.each do |idea| %>
    <tr>
      <td><%= idea.Id %></td>
      <td><%= idea.Subject %></td>
    </tr>
  <% end %>
</table>
</div>
