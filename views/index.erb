<div class="container">
  <table class="table table-striped table-bordered">
  <tr>
    <th>Id</th>
    <th>Subject</th>
  </tr>

  <% @ideas.each do |idea| %>
    <tr>
      <td><%= idea.id %></td>
      <td><%= idea.subjec %></td>
    </tr>
  <% end %>
</table>
</div>
